#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define fastio ios::sync_with_stdio(false); cin.tie(nullptr);

// a ^ b ^ b ^ c = a ^ c
void solve() {
    int n;
    cin >> n;  
}

int main() {
    fastio;
    int t;
    cin >> t;
    while (t--) solve();
    return 0;
}